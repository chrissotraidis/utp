# UT99Apple status

- **State:** INVESTIGATING
- **Current PRD gate:** G2 physical-device host shell remains unproven; iPadOS/iOS Simulator stock DeathMatch, full-bleed EctoPad-derived composition, and actual FruCoRe presentation instrumentation are passing partial evidence
- **Last passing gate:** G1 complete native-image/dependency feasibility audit passes for all eight v469e Mach-O images; later simulator execution remains partial evidence while G2 requires a physical device
- **Hypothesis:** The verified real-FMOD iPhoneOS candidate and no-game-data IPA workflow are ready for physical promotion; the remaining uncertainty is Apple signing/device execution and genuine hardware input/performance behavior, not Simulator composition or package assembly.
- **Current blocker:** `make doctor` now reports zero connected physical iOS/iPadOS devices, zero Apple Development identities, and no configured project development team. The simulator reaches v469e login, player possession, SDL viewport creation, Frucore initialization, `Game engine initialized`, and the main loop using a reproducibly retargeted iOS Frucore library. The touch UI is measured from EctoPad: fixed movement/aim sticks, dominant FIRE/ALT cluster, JUMP/DUCK tier, four-way SCORE/USE/PREV/NEXT D-pad, original Unreal MENU, and separate host menu. Eleven-control iPad, iPhone, and southpaw geometry tests pass. Live assistive FIRE produces ordered SDL down/up edges, and synchronous host-menu activation opens the real menu on the main thread while SDL is running. Manual physical finger interaction, simultaneous multi-touch, reach, haptics, and a touch-only bot match remain unproven. UIKit bridges cover hardware keys and pointer-hover deltas, but physical keyboard, mouse, controller, audio, safe-area, and performance behavior remain unproven. The known custom-map/network and audio hardware gaps are unchanged.
- **Next experiment:** Physical G2 is the next promotion experiment: attach exactly one trusted Developer Mode iPhone/iPad, configure an Apple Development identity/team, run the guarded signed build/install path, then collect engine log, first-frame screenshot, touch-only match, menu, Files/share, audio, controller, rotation, and performance evidence.
- **Exact command:** Attach and trust exactly one Developer Mode iPhone/iPad, configure an Apple Development identity, then run `DEVELOPMENT_TEAM=<team-id> make device-check && DEVELOPMENT_TEAM=<team-id> make device-run`; after importing data, run `UT99_DEVICE_AUTOSTART=1 DEVELOPMENT_TEAM=<team-id> make device-run`.
- **Active device/destination:** iPad Air 11-inch (M4) Simulator `F05D2D40-0A01-47C9-9BD7-0C0E19F7512C`; the final-source real-FMOD package is running a live remote `CTF-LongGuns` session entered through the original stock browser as the sole simulator/client process.
- **Latest evidence:** `docs/evidence/network/2026-08-24-original-browser-join/RESULT.md` records the original-browser-to-live-session pass; clean-checkout package proof remains in `docs/evidence/reproducibility/2026-08-24-clean-checkout/RESULT.md`. Physical-device readiness is `docs/evidence/ios-shell/2026-08-24-device-readiness/`; aggregate host-Metal evidence is `docs/evidence/ios-shell/2026-08-24-g2-metal-smoke/RESULT.md`; latest static gate evidence is `docs/evidence/engine-startup/2026-08-24-g1-complete-native-audit/`.
- **Branch/commit:** `main`; use `git rev-parse HEAD` for the authoritative tracked revision.
- **Proprietary local inputs:** Official v469e DMG and sanctioned GOTY ISO are present under ignored `ref/`; canonical data pack is generated under ignored `build/UT99Data*`.
- **Reference UI:** EctoPad at commit `461de17f549d98742bc3b2d031156f79ab3eaa9d`; documented in `docs/REFERENCE_ECTOPAD.md`. GoldenPad is historical only.

Latest reproducibility follow-up: the formerly untracked implementation is now a complete tracked source snapshot. A separate no-shared-worktree clone of `e61023d` freshly passed pinned bootstrap, macOS v469e baseline preparation, the full test suite, and the verified real-FMOD iPhoneOS package build. The five required SDL/UIKit changes are now a tracked patch applied only to an ignored generated source copy; the pinned `ref/SDL2` checkout remains pristine. Exact artifact and patch hashes are recorded in `docs/evidence/reproducibility/2026-08-24-clean-checkout/RESULT.md`. Physical signing and G2 remain open.

Latest browser-join follow-up: an opt-in point-accurate probe now continues from the populated stock `UT Servers` tab through a real original-browser row double-click. The selected public endpoint completed the v469 challenge, advertised and downloaded its custom packages, loaded `CTF-LongGuns`, logged in, and possessed the player. The sole iPad Simulator/client is visibly running the live Wildcard's SiegeXtreme session with the touch layer composed. Evidence is in `docs/evidence/network/2026-08-24-original-browser-join/RESULT.md`. Physical finger/device networking and extended movement/fire/respawn/chat/map-transition/disconnect coverage remain open.

Latest completion-audit follow-up: the PRD-required stable commands now exist and pass for pinned bootstrap, macOS baseline, hosted harness alias, diagnostics collection, package creation, and runtime cleanup. The unreachable legacy custom host menu was removed. A fresh rebuild caught a real-FMOD load-time black screen that static verification had missed: macOS FMOD still named the non-loadable iOS `AudioUnit.framework` runtime image. The transform now binds those symbols through iOS `AudioToolbox.framework`, package verification rejects the invalid dependency, the real-FMOD Simulator package reaches `Running` with visible Deck16 rendering, and the repaired iPhoneOS package rebuilds/verifies. Current capture: `docs/evidence/engine-startup/2026-08-24-ios-ectopad-menu-redesign/18-current-source-real-fmod-running.png`. The honest release checklist is `docs/COMPLETION_AUDIT.md`; physical G2 remains open.

Latest touch-copy correction: the rejected installed build exposed the internal reference name as a preset, duplicated size controls, left gameplay buttons underneath the settings panel, and still carried the wide NEXT trigger. The current source was rebuilt and reinstalled. The settings panel no longer exposes any preset concept; it offers direct opacity, size, handedness, and controller-auto-hide controls plus arrange/save/restore actions, and suppresses gameplay controls while open. PREV/NEXT remain compact utility-pad directions during play. Side-by-side evidence and the passing QA report are in `docs/evidence/engine-startup/2026-08-24-ios-control-settings-copy-fix/` and `design-qa.md`. The refreshed diagnostic IPA SHA-256 is `b6f93be8fc19de97dba7c9229e1bdc9ff2ae81f90b4bcf73dbf27c72b59d4625`; it contains no user game data and remains intentionally non-installable on stock iOS without signing.

Latest packaged-settings verification: the auto-start screenshot hook no longer opens touch settings before the renderer overlay transition, which previously let the renderer hide the panel and made fresh builds ambiguous. The rebuilt real-FMOD Simulator package now presents the actual in-game panel after host overlay preparation. It visibly contains no preset row or reference-project name, and all gameplay controls are suppressed beneath it. The package returned to the default gameplay layer and remains running as the sole client on the sole booted iPad Simulator. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-player-facing-controls-fix/` and `design-qa.md`.

Latest package-integrity follow-up: repeated package builds exposed a nested generated `UT99Data/UT99Data` directory. All four package targets now merge the prepared data pack's contents into the destination instead of recursively copying its parent directory, and package verification rejects any nested recurrence. The generated duplicate was quarantined outside the repository, then the real-FMOD iPhoneOS and Simulator candidates rebuilt and passed verification. No source or owner data was removed.

Latest multiplayer follow-up: the dead informational Multiplayer alert is replaced by a native direct-connect sheet. It rejects non-Unreal schemes, normalizes a bare hostname, remembers the last valid address, and routes active sessions to the original Unreal Tournament menu. A current-source iPad run entered `ut99.weba.ru:7777` through this UI; the original engine recorded DNS resolution, `CHALLENGE VER=469`, download managers, `WELCOME LEVEL=DM-CMetal`, and the remote map load, then rendered the live server. Evidence is `docs/evidence/network/2026-08-24-native-direct-connect-ui/RESULT.md`. Master-query population and a meaningful multi-player match remain open; Steam-origin import also remains untested because no local UT99 Steam installation is available and the configured secondary Steam volume is unmounted.

Latest original-browser experiment: the stock v469e `Multiplayer → Find Internet Games` window opens with fully localized tabs and now accepts point-accurate touch/pointer input. The failed Retina-scaled attempt exposed UWindow's additional `MouseScale=0.6`; the Apple profile now uses `MouseScale=1.0`, delivers logical SDL points to the focused window, and sends motion before button edges. The captured original `UT Servers` tab is selected and visibly lists 775 servers; the original engine log records community-master resolution, GameSpy/HTTP browser links, and individual-server ping traffic. This bounded Simulator slice passes in `docs/evidence/network/2026-08-24-original-server-browser/RESULT.md`; physical finger selection and browser-driven joining remain open and G8 is not promoted from Simulator evidence.

The DMG and all generated audit output are local evidence only and are not distributable project source.

Latest follow-up: the three-dot menu now contains the required Resume, Controls, Touch Layout, Graphics, Audio, Multiplayer, Game Data, Diagnostics, and About sections. Controls persist look sensitivity and Y inversion and affect the live touch-look bridge; Touch Layout persists opacity and global scale; Graphics persists safe-texture compatibility and FruCoRe VSync for the next start while reporting actual renderer metrics; Audio persists engine-audio enablement for the next engine start, with route reactivation available immediately. The touch layer separates the original Unreal `MENU`/Escape action from the host three-dot menu. Genuine manual drag/resize persistence, physical frame pacing, and physical iPad validation remain open.

Latest network follow-up: an explicit clean request for `unreal://ut99.weba.ru:7777/DM-Unreality][` reached the public endpoint and rendered a live server session, but the endpoint rotated to `DM-Aim` rather than serving the requested historical map. The native server UI and GoldenPad rail are visible in `dm-unreality-request-pass.png`; the exact map remains unavailable from this endpoint and is not marked passed.

Latest input/package follow-up: the UT-specific controller semantic layer now maps shoulder buttons to PREV/NEXT weapon and the optional controller Options button to the original Unreal game menu while retaining the controller Menu button for the host menu. A clean one-simulator run reached `DM-Deck16][`, entered the original front end/main loop, delivered the complete touch action smoke including Escape-backed `MENU`, and captured the composed original UI plus touch rail in `docs/evidence/engine-startup/2026-08-24-ios-sim-input-pass/touch-controller-menu-pass.png`. Both device-target packages rebuilt and verified. Physical controller callbacks, physical touch-only play, and physical renderer/audio remain unproven because no hardware is attached.

Latest lifecycle follow-up: using the Simulator GUI Home control and reopening the app produced `UT99 lifecycle: resign-active` followed by `UT99 lifecycle: active`; the resumed frame remained in the live Deck16 scene and the run had one engine entry with no duplicate/fatal record. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-sim-lifecycle-pass/`. Physical suspend/resume and audio-route behavior remain unproven.

Latest visual follow-up: a true landscape Simulator GUI pass now shows the full aligned 4:3 Deck16 canvas and the right-side UT rail at the intended GoldenPad-derived positions, including distinct host-menu and original-Unreal `MENU` surfaces. The raw `simctl` screenshot exporter remains rotated, so `landscape-gui-pass.png` is the authoritative visual capture. Physical-device framing and touch scale remain open.

Latest audio follow-up: the 280-buffer-failure baseline is superseded. OpenAL Soft's pre-macOS aligned-allocation fallback was incorrectly selected for iOS because `AvailabilityMacros.h` publishes a legacy macOS compatibility value there; low-alignment objects consequently failed `posix_memalign`. The generated iOS source copy now limits that fallback to `TARGET_OS_OSX`, and the dependency output path now matches the directory embedded by device packages. A clean one-simulator run held at zero allocation errors, initialized ALAudio/OpenAL Soft 1.25.2, entered the original main loop, and logged `ALAudio: playing Godown Go Down`. Both device packages rebuilt and verify with the corrected OpenAL symbols. Audible hardware output, effects fidelity, interruption/route recovery, and physical G6 remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-openal-buffer-pass/`.

Latest touch-rail follow-up: the user-provided iPad capture showed the UT action rail was still too small and low-contrast. The rail now uses larger targets with persistent UT semantic colors, and the default GoldenPad profile uses 0.86 opacity so the colors remain readable without forcing high-visibility mode. A fresh one-simulator Deck16 visual pass reached the ready-signals scene and captured `docs/evidence/engine-startup/2026-08-24-ios-ut-rail-current-pass/ut99-default-opacity-gameplay.png`. Physical touch accuracy, safe-area sizing, and renderer behavior on real hardware remain open.

Latest iPhone follow-up: the first iPhone simulator run exposed a narrow-scene collision: a full-height 4:3 frame left only 54 logical points for controls. The host, SDL UIKit framing, Apple viewport profile, and GoldenPad overlay now share a phone-specific compact bay policy, and auto-start waits for actual landscape scene orientation. A fresh iPhone 17 simulator run captured `docs/evidence/engine-startup/2026-08-24-ios-iphone-landscape-pass/iphone17-gameplay-phone-bay-score-fixed-landscape.png` with the rail visible and the original Deck16 scene active. Physical iPhone behavior remains open.

Latest regression follow-up: the final simulator package was run fresh on the iPad Air 11-inch after the phone geometry changes. The iPad retained the intended full-height 4:3 canvas and colored right-side UT rail; evidence is in `docs/evidence/engine-startup/2026-08-24-ios-ipad-regression-pass/`. No simulator is currently active.

Latest diagnostics follow-up: the Diagnostics section now reports the actual host/runtime state and supports bounded log ZIP export, clipboard copy, host-config reset, and safe-texture mode for the next launch. The fresh iPad smoke reached the original title/menu screen with the rail visible; it is not a populated-match or physical-touch claim.

Latest input-contract follow-up: the final simulator package was compared against the staged UT99 `User.ini` and delivered the complete FIRE/ALT/JUMP/USE/DUCK/NEXT/PREV/SCORE/MENU plus movement/look smoke inside the original Deck16 main loop. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-touch-contract-pass/`; this remains semantic bridge evidence rather than physical touch completion.

Latest renderer follow-up: the SDL/UIKit boundary no longer presents a legacy 512×384 or cropped 1280×800 game surface. The Apple profile derives a 4:3 mode, SDL publishes 1024×768, and the renderer root is aspect-preserved on the left while the right side remains the GoldenPad control bay. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-native-viewport-pass/`; physical-device scaling and performance remain open.

Latest resolution/UI follow-up: the 4:3 island and detached black control bay are superseded. A modern launch-screen declaration gives the iPad Air its native 1180×820 scene; SDL's first Metal drawable query now returns 2360×1640 and 2× mouse scale, so FruCoRe fills the device instead of drawing into one quarter of the Retina surface. The touch layer uses GoldenPad's actual scale and neutral idle/pressed-tint treatment, and menu smoke restores the prior profile. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-fullbleed-ui-pass/`. Physical hardware remains open.

Latest phone-UI follow-up: the full-bleed iPhone 17 run exposed that the tablet action geometry itself—not renderer resolution—caused the visible collisions. A dedicated phone layout now uses a measured ALT/FIRE/JUMP stack, a separate USE/DUCK/NEXT/PREV bottom rail, compact movement/look hit zones, and safe-area-relative MENU placement. The true-landscape GUI capture has no intersecting controls at 874×402 points / 2622×1206 pixels, and a subsequent one-runtime iPad Air regression retained 1180×820 / 2360×1640 full-bleed rendering. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-iphone-fullbleed-ui-pass/`. Physical touch reachability remains open; no simulator is active.

Latest renderer-metrics follow-up: the old Graphics “frame cap” controlled only the dormant host `MTKView` and has been removed. The Metal shim now measures FruCoRe's real `presentDrawable:` stream and exposes average FPS, 1% low, frame time, frame count, and drawable size to Graphics/Diagnostics. Two clean one-simulator runs retained a full-bleed 2360×1640 drawable and non-overlapping iPad controls. VSync-off measured 131.35 FPS at 10 seconds; FruCoRe `UseVSync=True` measured 133.26 FPS, proving that Simulator does not pace this path to 60 Hz. These numbers are simulator-produced-frame diagnostics only; physical G9 remains open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-metal-performance-pass/`; no simulator is active.

Latest data-integrity follow-up: the earlier importer staged input but merged files into the live installation one by one, so its “transactional” claim was incomplete. Imports now create a complete last-known-good backup and atomic phase journal before replacing the four user-content directories and manifest. A forced two-item interruption recovered the old data, a subsequent replacement verified 4/4 fixture hashes, the generated System fixture survived both paths, and no journal/staging/backup debris remained. The host also records explicit PRD states, blocks a duplicate engine start, removes the host panel before first SDL presentation, and correctly reports the embedded engine path. One clean simulator run recorded one engine entry/main loop and retained full-bleed non-overlapping Deck16 UI. Physical picker/storage behavior remained open at this point; cancellable long-copy handling is addressed by the import-UX follow-up below. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-data-transaction-pass/`; no simulator is active.

Latest import-UX follow-up: folder and ZIP preparation now runs off UIKit's main queue and reports discovery/extraction/validation phase, current file, exact count, and determinate progress in a centered modal card. Cancel remains enabled through staging and streaming SHA-256 work, then disables at the journaled install boundary. A deterministic cancellation after one prepared file preserved the installed content and manifest and left no transaction debris; a separate hash test interrupted between 1 MiB chunks. The accepted iPad Simulator Save Screen output is 2360×1640, and a subsequent clean engine launch retained full-bleed gameplay and separated controls. All simulator/device packages rebuilt and verified. Physical Files providers, low-storage behavior, and cancellation latency remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-import-progress-pass/`; no simulator is active.

Latest touch-refinement follow-up: the rejected icon-tile and text-only debug-circle iterations are superseded. A custom `UT99TouchActionButton` now owns a true circular ultra-thin material face, inset semantic ring, dominant action glyph, restrained micro-label, haptic/scale feedback, and press-only accent fill without `UIButton.Configuration`, shadows, or rounded tiles. The tablet thumb arc and dedicated compact phone rail share a collision solver; High Visibility × 135% remains separated on both reference canvases. Fresh true-landscape Simulator Save Screen captures verify the default iPad gameplay layer, the editor with visible MOVE/LOOK affordances, and the iPhone 17 Pro Max layout clearing the Dynamic Island. Simulator, stub-device, and real-FMOD device packages rebuilt and verified sequentially. Physical reach, multi-touch, drag/pinch persistence, and hardware haptics remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-touch-refinement-pass/`; no simulator is active.

Latest audit follow-up: schema-v2 recursive discovery now covers all eight Mach-O images in the pristine v469e app, not only the main executable. All eight contain ARM64, are unencrypted, and have no writable-executable segment; both executables have `LC_MAIN`; all 61 dependency edges have bounded iOS dispositions; and configured stock Unreal packages imply no additional native package dylibs. G1 static feasibility now passes. G2 remains the current gate because no physical iPhone/iPad is attached. Evidence is in `docs/evidence/engine-startup/2026-08-24-g1-complete-native-audit/`.

Latest touch-minimal follow-up: the custom material-ring pass is superseded after a live Product Design comparison against GoldenPad's current `MomentaryAction`. The overlay now uses the same restrained construction: one 46%-black circular face, one 34%-white hairline, one centered semantic mark, and action color only while pressed. FIRE/ALT/JUMP/USE/DUCK/PREV/NEXT use single UT labels; SCORE and original-Unreal MENU use one utility glyph each. The GoldenPad profile returns to the reference 0.72 opacity, and the tablet action arc was tightened while preserving High Visibility × 135% collision-free fitting. Accepted true-landscape GUI captures show a coherent iPad rail and an iPhone rail clearing the Dynamic Island. Simulator, stub-device, and real-FMOD device packages rebuilt and verified sequentially. Physical finger reach, multi-touch, and haptic feel remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-touch-minimal-pass/`; no simulator is active.

Latest touch-hierarchy follow-up: the source-faithful text circles are now superseded because seven equal-weight labels still read as a debug rail. The live layer is icon-only, with one dominant FIRE target, a smaller ALT/USE/JUMP/DUCK thumb arc, and a joined PREV/NEXT weapon switcher; accessibility labels preserve the full UT semantics. The default profile is 0.84 opacity for map-independent readability. Accepted true-landscape captures show the final iPad and iPhone layouts, High Visibility × 135% remains collision-free, and assistive FIRE activation entered first-person play while recording ordered pressed=true/false SDL edges. Simulator, stub-device, and real-FMOD device packages rebuilt and verified sequentially. Physical finger reach, multi-touch, VoiceOver traversal, and haptic feel remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-touch-hierarchy-pass/`; no simulator is active.

Latest recovery follow-up: the host now persists a bounded active-session marker before original entry, recovers surviving markers into Crashed state, offers Safe Mode/Normal/Diagnostics choices, and archives controlled/nonzero returns while restoring the host surface. A one-iPad Simulator sequence recovered a seeded normal session, launched the real engine through Safe Mode with safe textures/VSync on and audio off, force-terminated that safe-mode process, and correctly identified the safe-mode interruption on relaunch with no stale active marker. Recovery JSON records are included in diagnostic ZIP assembly. Simulator, stub-device, and real-FMOD device packages rebuilt and verified sequentially. Physical crash reports, watchdog/OOM distinction, controlled engine return, and device recovery behavior remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-crash-recovery-pass/`; no simulator is active.

Latest diagnostic-export follow-up: ZIP generation is now a standalone validated component rather than a private share-sheet helper. Deterministic tests reject empty/absolute/traversal/duplicate entry sets, verify redaction, and independently run `unzip -t`. The packaged iPad Simulator app wrote a 178,362-byte archive containing redacted diagnostics, full engine stdout, and the prior recovery JSON; the pulled archive passed CRC/central-directory inspection and a whole-archive account/secret scan, and reports the exact embedded engine SHA-256. Simulator, stub-device, and real-FMOD device packages rebuilt and verified sequentially. The physical share sheet, Files destination, and exported-file round trip remain unproven because no device is attached. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-diagnostics-export-pass/`; no simulator is active.

Latest touch-configuration follow-up: the faint icon-only pass is superseded. Actions now use translucent gradient circles with semantic glyphs plus compact rounded captions, a dominant FIRE face, a deliberate thumb arc, and the joined weapon rail. Persisted configuration adds left-handed mirroring, per-action visibility, acceleration, movement/look dead zones, and controller auto-hide; live-test mode keeps gameplay active while showing restrained MOVE/LOOK guides. Screenshot iteration found and fixed a southpaw MENU/banner collision and a 180-degree relaunch caused by forcing one landscape side. Accepted iPad/iPhone GUI captures and a normal-mode persistence relaunch pass; right/left High Visibility × 135% geometry passes on both canvases. Simulator, stub-device, and real-FMOD packages rebuilt and verified sequentially. Named profiles, optional gyro, and all physical-touch/device gates remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-touch-configuration-pass/`; no simulator is active.

Latest touch-profile follow-up: the caption-heavy gradient pass is superseded. Gameplay controls now have small icon-only visual faces inside larger unchanged hit targets, one dominant FIRE face, a restrained combat arc, and a joined weapon rail. The host menu hides gameplay controls while presented. Versioned `.ut99touch` profiles save, validate, import, export, apply, delete, and round-trip placement, scale, visibility, handedness, and aim tuning with bounded storage. Fresh iPad Air 11-inch and iPhone 17 Pro Max runs fill 2360×1640 and 2868×1320 native landscape targets without control overlap; the compact phone layout clears the Dynamic Island. `make test`, the simulator package, the diagnostic device package, and the real-FMOD device package pass. Physical finger reach, simultaneous touch, VoiceOver traversal, haptics, and optional gyro remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-icon-touch-profile-pass/`; no simulator is active.

Latest EctoPad-baseline follow-up: all prior GoldenPad visual/default-layout decisions are superseded. The active profile uses EctoPad's measured fixed-stick and action hierarchy with UT-specific semantic adaptation, not its Metroid bindings. `make test`, the full transformed-engine simulator package, the diagnostic device package, and the real-FMOD device package pass. Fresh live iPad/iPhone captures show full-bleed rendering and coherent two-thumb zones; live assistive FIRE records ordered SDL down/up edges, and the host menu now opens synchronously on the main thread while the original loop runs. The final simulator package is installed and running on the sole booted iPhone destination. This is Simulator/assistive evidence, not a physical touch-only G7 pass. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-ectopad-baseline/`.

Latest device-readiness follow-up: the required doctor contract now distinguishes physical CoreDevice hardware from simulators, reports signing identity/team readiness without printing identities, verifies all required tools and canonical inputs, and names EctoPad rather than the superseded GoldenPad reference. A new `device-check`/`device-build`/`device-install`/`device-run` sequence selects exactly one physical iOS/iPadOS device, enforces the single-runtime policy, builds the real-FMOD candidate with automatic provisioning, verifies the package, installs it, and launches either the host or an opt-in automated match. Deterministic tests pass. Current readiness is correctly blocked before mutation with `physical_ios_ipados_devices=0`, `signing_identity_count=0`, and no configured team. Evidence is in `docs/evidence/ios-shell/2026-08-24-device-readiness/`.

Latest G2-host follow-up: the shell's `MTKView` no longer has an empty delegate. It now commits and presents a real Metal command buffer after layout, records completion status/device/drawable, and feeds that log into diagnostics. The aggregate `-UT99G2SmokeTest` sequences transactional import, EctoPad default presentation, host Metal, and diagnostics export, then writes a final marker only when all four pass. A fresh iPhone 17 Pro Max Simulator run reported `passed=true metal=true importer=true diagnostics=true ectoPad=true` at 2868×1320; the diagnostics ZIP independently passes CRC inspection and contains the Metal log. Computer Use opened the complete host menu while gameplay controls hid, then the latest package returned to the original bot-match renderer. `make verify-device` now performs the same automated collection on physical hardware but cannot run until device/signing prerequisites exist. Evidence is in `docs/evidence/ios-shell/2026-08-24-g2-metal-smoke/`.

Latest EctoPad visual-QA follow-up: the baseline now uses EctoPad's exact `0.82` opacity and explicit 42%-diameter stick thumbs. The phone MENU caption survives compact sizing. A combined source/implementation review then rejected EctoPad's intentional GameCube A/B/X/Y overlap for separate UT actions; the accepted iPad adaptation retains EctoPad sizes and hierarchy while opening FIRE/ALT/JUMP/DUCK/aim and moving the utility pad away from movement. Fresh final-source iPad Air 11-inch and iPhone 17 Pro Max live captures are full-bleed, legible, and collision-free. `make test`, simulator packaging, diagnostic device packaging, and real-FMOD device packaging pass sequentially. Physical reach, simultaneous multi-touch, haptics, and touch-only gameplay remain open. Evidence is in `docs/evidence/engine-startup/2026-08-24-ios-ectopad-visual-qa/` and `design-qa.md`.

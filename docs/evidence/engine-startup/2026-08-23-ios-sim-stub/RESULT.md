# iPadOS simulator original-entry experiment — 2026-08-23

**Classification: PASS for original engine entry, Frucore initialization, main-loop startup, visible CityIntro and stock DeathMatch frame compositing, native landscape presentation, and the GoldenPad-derived UT control surface on iPadOS Simulator; bot-populated touch-only gameplay, audio, and physical-device geometry remain unproven.**

- Target: iPad Air 11-inch (M4) Simulator, iOS 26.5, UDID `F05D2D40-0A01-47C9-9BD7-0C0E19F7512C`.
- Package: `build/ios-engine-sim-app/Build/Products/Debug-iphonesimulator/UT99Apple.app`.
- The package embeds the transformed arm64 simulator engine, SDL2, rebuilt OpenAL/XMP/mpg123/libsndfile libraries, distinct legacy API shims, and the re-exporting Metal shim.
- `tools/verify_ios_package.sh` passes.
- The app was launched with `-UT99AutoStart`; console evidence reports `Original engine entry started`.
- The host launch now prepares the bundled GOTY data under Application Support, changes the engine working directory there, calls `SDL_SetMainReady()` before direct entry invocation, and passes `-log -nosound` to the original entry. The captured engine log reaches login, possession, SDL viewport creation, Frucore initialization, `Game engine initialized`, and `Entering main loop` using the reproducibly retargeted iOS Frucore library.
- The host includes a GoldenPad-derived UIKit touch layer using the reference iPad normalized layout and modern-preset visual behavior: transparent move/look hit zones, guide-only-on-demand movement feedback, tinted translucent circular UT99 action buttons, bright reference rings, and press tinting.
- The touch adapter now maps those semantic actions and analog deltas to SDL2 events through `SDL_PushEvent` when the embedded engine is loaded. Diagnostic `-UT99TouchSmokeTest` drove the same semantic action path against the live engine loop, including primary/alternate fire, jump, use, crouch, weapon cycling, scoreboard, movement, and look; [`touch-smoke.log`](touch-smoke.log) records the sequence. This proves the live host-to-SDL bridge path, but no physical-finger or touch-only game match is claimed.
- The simulator was used alone and shut down afterward; runtime guard reports zero booted simulators.

The simulator package uses the explicit no-audio FMOD shim because the real bundled FMOD image depends on an AudioUnit path unavailable in the iOS Simulator runtime. The real-FMOD iOS device candidate is built separately by `make ios-engine-real-package`. The capture at `ut99-engine-touch-overlay-final.png` shows live CityIntro pixels beneath the host controls. The controls sit above SDL, use the GoldenPad tinted-circle visual language and normalized iPad placement, and use UT99 bindings: left/right mouse for Fire/AltFire, Space for Jump, Enter for Use, C for Duck, wheel events for weapon cycling, and F1 for scoreboard.

A separate `-UT99AutoStart -UT99AutoMatch` run reached `DM-Deck16][`, `Botpack.DeathMatchPlus`, player possession, `Game engine initialized`, and `Entering main loop`. A later accessibility tap on the actual FIRE control started the original match and showed the live HUD with `Rank 1/5`; [`ut99-touch-started-bot-match-filled.jpeg`](ut99-touch-started-bot-match-filled.jpeg) records that state. This proves simulator UI automation, not physical touch-only gameplay.

Evidence: `ut99-engine-touch-overlay-final.png`, `touch-smoke.log`, `device-console-input-bridge.log`, `ios-sim-engine-input-bridge.png`, `device-console-goldenpad.log`, `ios-sim-engine-system.png`.

The host picker now accepts folders and ZIP archives. ZIP imports use a small in-target reader for stored/raw-deflate entries, reject encrypted or unsafe paths and desktop executables, extract into a temporary staging tree, then produce a SHA-256 manifest. The native Files picker was exercised on the simulator: a data-only archive completed with `Imported 283 UT99 content files`, while the macOS installer archive was rejected for executable content as designed. [`ut99-zip-picker-imported-283-files.jpeg`](ut99-zip-picker-imported-283-files.jpeg) records the successful result. This historical run staged before merging but did not prove atomic commit/rollback; the later `2026-08-24-ios-data-transaction-pass` supersedes that claim with a backup-and-journal recovery test.

The Simulator was rotated to landscape before the rebuilt run. The host and SDL scene now negotiate the same landscape orientation, and the original renderer fills the gameplay canvas with its 4:3 aspect preserved while the GoldenPad controls remain aligned to that canvas. [`ut99-landscape-device-rotation-readable.png`](ut99-landscape-device-rotation-readable.png) records the corrected presentation.

The GoldenPad tablet geometry pass now uses the reference 1366x1024 canvas and 1.18 tablet scale for the move/look zones and action faces. A clean one-simulator auto-match run shows the scaled controls over the original Deck16 frame in [`ut99-goldenpad-geometry-landscape.png`](ut99-goldenpad-geometry-landscape.png). This is improved simulator fidelity, not physical touch-only gameplay evidence; physical device frame and finger interaction remain open.

An opt-in `-UT99AudioEnabled` run reached `ALAudio`, selected `OpenAL Soft (CoreAudio Default)`, and began playing the UT music track. A clean run with every audio section normalized to 16 effect channels reported 280 voice-buffer allocation failures; lower channel profiles were worse. This remains partial audio evidence rather than a passed effects/voice gate.

The host-input safety pass adds an explicit release path for active touch actions when the host menu opens, the app resigns active, or a real controller takes over. The rebuilt simulator package passed verification, and a one-simulator `-UT99TouchSmokeTest` run completed the same FIRE/ALT/JUMP/USE/DUCK/NEXT/PREV/SCORE plus movement/look sequence; [`ut99-touch-release-safety.png`](ut99-touch-release-safety.png) records the resulting live frame. This verifies the bridge and release wiring, not physical touch behavior.

The host menu now exposes persisted touch profiles: GoldenPad default, Compact, and High visibility. The profile selection scales the reference geometry and opacity through `UserDefaults`; the clean one-simulator auto-match/smoke run after the change completed and produced [`ut99-touch-profiles.png`](ut99-touch-profiles.png). The simulator UI rendered the profile action sheet with all three choices. A clean terminate/relaunch with the persisted Compact preference then showed `Touch layout: Compact` in the live host menu and produced [`ut99-touch-compact-relaunch.png`](ut99-touch-compact-relaunch.png) and [`ut99-touch-compact-menu.png`](ut99-touch-compact-menu.png). Direct action-sheet tap selection could not be confirmed because Simulator pointer capture intercepted the injected click; physical iPad selection and touch behavior remain open.

The subsequent window-routing pass keeps SDL's secondary renderer window non-interactive and restores the host window as the intended touch/menu owner, while preserving the rendered SDL surface. Simulator, iOS-device, and real-FMOD packages all rebuilt and package verification passed. The fresh SDL session reached `Original engine entry started` and produced [`ut99-touch-window-routing.png`](ut99-touch-window-routing.png), but the desktop accessibility service timed out before a fresh host-menu tap could be confirmed; this remains an implementation change awaiting direct UI verification.

The deterministic `-UT99MenuSmokeTest` then exercised the actual host menu construction and `GoldenPadTouchOverlay.applyTouchProfile(.compact)` path during a live original-engine run. It reported `menuConstructed=true profile=compact persisted=true`; [`ut99-menu-smoke.log`](ut99-menu-smoke.log) records the result. This validates the menu/profile state machine, not a physical finger tap or a successful accessibility click.

The next GoldenPad visual pass aligns the idle state with the pinned reference implementation: action faces are neutral translucent black until pressed, pressed faces take the control tint, the movement guide is hidden at rest, and the look guide is revealed only during a look drag. The live engine frame in [`ut99-goldenpad-reference-visual-pass.png`](ut99-goldenpad-reference-visual-pass.png) records the updated simulator presentation; this is simulator visual evidence, not physical touch-comfort evidence.

The subsequent SDL geometry pass resizes SDL's secondary `UIWindow` and root view to the active host scene before the GoldenPad overlay is composited, addressing the small upper-left renderer seen in the earlier portrait capture. The simulator package and both iOS device packages rebuilt and verified. A fresh CLI framebuffer was captured as [`ut99-window-geometry-pass.png`](ut99-window-geometry-pass.png), but it remained in the portrait device buffer because the simulator was not rotated through the GUI; that image is retained as diagnostic evidence only, and the corrected landscape composition remains pending direct simulator rotation.

The direct GUI rotation pass then verified the corrected composition: the full Deck16 renderer fills the landscape iPad scene, and the UT-specific GoldenPad rail is readable with teal ALT, orange FIRE, green USE/JUMP, yellow DUCK, and blue PREV/NEXT faces. The captured frame is [`ut99-goldenpad-colored-landscape.png`](ut99-goldenpad-colored-landscape.png). The color treatment was corrected from the overly dark idle faces while preserving hidden-at-rest move/look guides and press tinting.

A fresh one-simulator interaction pass then used the actual accessibility-exposed GoldenPad controls after the visual change. FIRE transitioned the original match from “Press Fire to start” into live first-person Deck16 gameplay; ALT, USE, JUMP, DUCK, NEXT, PREV, and SCORE were subsequently tapped against the running engine, with the HUD/rank state changing during the sequence. [`ut99-touch-action-sequence-landscape.png`](ut99-touch-action-sequence-landscape.png) records the active landscape frame. This remains simulator UI automation evidence, not physical finger-only proof.

After that visual/input pass, the reverted simulator package was rebuilt and run once in a clean single-runtime `-UT99AutoStart -UT99AutoMatch -UT99TouchSmokeTest` session. The original renderer reached a populated Deck16 scoreboard, `Game engine initialized`, and `Entering main loop`; the actual semantic touch-smoke actions reached the live bridge. [`ut99-post-audio-revert-normal.png`](ut99-post-audio-revert-normal.png) and [`post-audio-revert-engine.stdout`](post-audio-revert-engine.stdout) record the run. A targeted opt-in ALAudio experiment setting `UsePrecache=False` was also tested, but it caused repeated voice-buffer allocation failures and was reverted; the audio classification remains partial and the prior 16-channel result remains the reference baseline.

The next GoldenPad fidelity pass corrected the action-face state mismatch: idle controls are now neutral black translucent circles, while FIRE/ALT/JUMP/USE/DUCK/NEXT/PREV receive their UT tint only during press, matching the pristine reference behavior. The rebuilt simulator, iOS-device, and real-FMOD device packages all passed verification. A clean single-runtime `-UT99AutoStart -UT99AutoMatch -UT99TouchSmokeTest` session reached the original engine main loop and emitted the complete semantic action sequence; [`ut99-goldenpad-black-idle-landscape.png`](ut99-goldenpad-black-idle-landscape.png), [`goldenpad-black-idle-engine.stdout`](goldenpad-black-idle-engine.stdout), and [`goldenpad-black-idle-touch-smoke.log`](goldenpad-black-idle-touch-smoke.log) record it. The CLI framebuffer is retained as engine evidence; direct physical touch and device framing remain open.

The window-routing follow-up found that SDL could recreate its renderer window after startup and leave the visible host controls exposed but unable to receive injected UIKit taps. `presentSDLWindowIfAvailable()` now resigns SDL's key status, reasserts the host window and overlay as interactive/key, and brings the host controls to the front after each attachment. The simulator package rebuilt and passed verification. In a clean single-runtime GUI session, the host menu opened, FIRE reached the original live first-person HUD/weapon frame, and the device was rotated through the Simulator UI to capture [`ut99-window-ownership-fire-landscape.png`](ut99-window-ownership-fire-landscape.png); [`window-ownership-fire-engine.stdout`](window-ownership-fire-engine.stdout) records `Game engine initialized`, `Entering main loop`, and the FIRE bridge event. This is authoritative simulator UI evidence, not physical-finger evidence.

The hardware-input follow-up adds a UIKit keyboard bridge for the stock UT99 bindings: W/A/S/D movement, Space jump, Enter use, C duck, F1-F12, arrows, Escape, and Tab are translated to the original SDL key symbols, with release handling on `pressesEnded` and `pressesCancelled`. Simulator and both device-target packages built and verified. A clean single-runtime simulator launch reached `Game engine initialized` and `Entering main loop`; the Simulator keyboard-capture setting was enabled and representative keys were sent, but this environment did not deliver `UIPress` callbacks to the app, so keyboard input is classified wired/UNVERIFIED pending a physical iPad. [`keyboard-bridge-engine.stdout`](keyboard-bridge-engine.stdout) records the engine startup evidence.

The pointer follow-up adds a non-cancelling `UIHoverGestureRecognizer` to the host overlay. Pointer deltas are normalized and sent through the existing relative SDL mouse-motion path, while touch look pans remain independent. Simulator and both device-target packages rebuilt and passed verification. A clean single-runtime `-UT99AutoStart -UT99AutoMatch -UT99TouchSmokeTest` run reached `Game engine initialized` and `Entering main loop`, with the live Deck16 frame captured in [`ut99-pointer-bridge.png`](ut99-pointer-bridge.png). Simulator pointer injection did not provide authoritative hover callbacks, and mouse-button mapping remains open; physical iPad trackpad/mouse evidence is still required. [`pointer-bridge-engine.stdout`](pointer-bridge-engine.stdout) records the engine run.

The renderer-fidelity follow-up corrected the iPad Metal presentation path. The SDL UIKit Metal view now keeps its native backing drawable while presenting the legacy 512x384 point fullscreen surface at the iPad 2x scale; the SDL view controller also reconciles its window with the attached host scene during layout. The simulator package rebuilt and verified, and one clean simulator run reached the original Deck16 loop. After rotating the actual Simulator UI to landscape, the game occupied the intended 4:3 gameplay canvas with the GoldenPad rail composited above it; [`ut99-render-layout-fix-simulator-window.jpeg`](ut99-render-layout-fix-simulator-window.jpeg) is the authoritative visual capture and [`render-layout-fix-landscape-engine.stdout`](render-layout-fix-landscape-engine.stdout) records the live engine evidence. This improves simulator framing but does not replace physical-device renderer and touch validation.

The controller-input follow-up fixes a real semantic bug in the host adapter: D-pad changes previously reread the left-stick axes, so a physical D-pad could not drive UT99 movement. The adapter now selects the changed element's axes, applies a small neutral dead zone, and explicitly publishes neutral relative-look input when a controller disconnects or takes over. Simulator, standard iOS-device, and real-FMOD device packages rebuilt and verified. A clean single-simulator auto-match/smoke run reached `Game engine initialized` and `Entering main loop`; [`ut99-controller-neutrality-pass.png`](ut99-controller-neutrality-pass.png) and [`controller-neutrality-engine.stdout`](controller-neutrality-engine.stdout) record the run. Physical controller behavior remains unclaimed.

The BC1 renderer follow-up adds a narrow Metal-shim fallback: BC1/DXT1 texture descriptors are created as RGBA8 textures and compressed upload regions are expanded before the original texture upload call. Simulator, standard iOS-device, and real-FMOD device packages rebuilt and passed verification. A clean simulator run recorded `UT99 BC1 fallback installed ... replace=true slice=true` in [`bc1-fallback-system.log`](bc1-fallback-system.log) and still reached `Game engine initialized` / `Entering main loop`; [`ut99-bc1-fallback-pass.png`](ut99-bc1-fallback-pass.png) is the matching frame. A follow-up direct connection to `ut99.weba.ru:7777` also installed the fallback and reached server welcome, map download, login, and possession on the server's current `DM-Agear` map; [`bc1-remote-engine.stdout`](bc1-remote-engine.stdout) and [`bc1-remote-system.log`](bc1-remote-system.log) record it. The historically failing `DM-Unreality][` package was not selected in this run, so broad custom BC1 compatibility remains open.

The GoldenPad UX follow-up adds a real host-menu “Edit touch layout” path. UT-specific FIRE/ALT/USE/JUMP/DUCK/NEXT/PREV/SCORE controls can be dragged and pinch-resized, normalized placements persist in `UserDefaults`, and reset returns to the reference tablet defaults. The clean simulator layout smoke constructed the real menu, entered the editor, installed the BC1 fallback, and reached the original engine loop; [`layout-editor-engine.stdout`](layout-editor-engine.stdout), [`layout-editor-system.log`](layout-editor-system.log), and [`ut99-layout-editor-pass.png`](ut99-layout-editor-pass.png) record the run. The command-line smoke cannot prove a genuine finger drag, and the captured framebuffer is portrait-buffer diagnostic evidence; manual landscape and physical touch validation remain open.

The final refinement makes the MOVE and LOOK zones pinch-resizable as well as repositionable. The exact rebuilt simulator package passed a final clean one-runtime launch; [`layout-editor-final-engine.stdout`](layout-editor-final-engine.stdout) records the editor transition and original main loop, [`layout-editor-final-system.log`](layout-editor-final-system.log) records BC1 installation, and [`ut99-layout-editor-final.png`](ut99-layout-editor-final.png) is the resulting framebuffer capture. All simulator runtimes were terminated afterward.

The follow-up GUI pass found and fixed two host-window regressions: SDL's secondary window could reclaim key status, and the host reassertion cleanup could hide a newly opened menu panel. The final clean one-simulator run opened the real host menu through the Simulator accessibility action, exposed Import UT99 data, GoldenPad profile, Edit touch layout, Reset touch layout, server, and diagnostics controls, then entered the editor. The editor exposed DONE plus MOVE/LOOK and all UT-specific action controls; the original renderer remained live underneath. [`touch-menu-editor-final.png`](touch-menu-editor-final.png) and [`touch-menu-editor-final-system.log`](touch-menu-editor-final-system.log) record the visible menu/editor pass. Manual finger drag/resize and physical iPad validation remain open.

The three-dot host menu now has the PRD section structure: Resume, Controls, Touch Layout, Graphics, Audio, Multiplayer, Game Data, Diagnostics, and About. The menu is scrollable, keeps direct connect/import/editor/probe/start actions, and routes the remaining sections to explicit status panels rather than silently doing nothing. A one-simulator launch with -UT99MenuSmokeTest -UT99LayoutSmokeTest constructed the 28-item menu, applied the persisted Compact profile, and entered the editor while the original engine was running. [`full-host-menu-landscape.png`](full-host-menu-landscape.png) is a raw simulator framebuffer diagnostic; its orientation is rotated by the simulator framebuffer exporter, while the GUI landscape capture remains the authoritative visual framing. The deeper preference editors and physical interaction gates remain open.

The settings follow-up makes the menu actions functional: Controls writes look sensitivity and Y inversion to UserDefaults and the live touch-look bridge reads them; Graphics writes the safe-texture compatibility preference for the next engine start; Audio writes the next-start engine-audio preference and can reactivate the current AVAudioSession route. The clean single-runtime launch with `-UT99MenuSmokeTest -UT99LayoutSmokeTest -UT99SettingsSmokeTest` reached the original engine entry, installed the BC1 fallback, built the 28-item menu, entered the editor, and recorded `UT99 settings smoke persisted=true` in [`settings-menu-validation-system.log`](settings-menu-validation-system.log). [`settings-menu-validation-raw.png`](settings-menu-validation-raw.png) shows the live Deck16 first-person frame with the UT-specific GoldenPad rail; framebuffer orientation remains diagnostic rather than authoritative GUI framing. Manual drag/resize, final visual tuning, and physical iPad gates remain open.

The fidelity pass added the remaining visible GoldenPad touch preferences: opacity and global scale are persisted and applied immediately. At that time Graphics also exposed a host-view frame-rate preference alongside the next-start texture compatibility switch; the later FruCoRe metrics pass proved that view was not the game renderer and superseded the control. The clean single-runtime launch reached the original engine, installed the BC1 fallback, constructed the expanded 29-item menu, entered the editor, and recorded `UT99 settings smoke persisted=true` in [`fidelity-pass-system.log`](fidelity-pass-system.log). [`fidelity-pass-raw.png`](fidelity-pass-raw.png) shows the rendered Deck16 waiting screen and UT-specific action rail. It is raw simulator framebuffer evidence; the GUI orientation caveat and physical-device gates remain unchanged.

The manual editor pass opened the real host menu through Simulator UI, entered the GoldenPad editor, and exposed the editable FIRE/MOVE/LOOK controls plus DONE. The live editor frame is captured in [`manual-editor-menu.png`](manual-editor-menu.png); the post-gesture frame is [`manual-editor-after-drag.png`](manual-editor-after-drag.png), and the clean relaunch frame is [`manual-editor-relaunch.png`](manual-editor-relaunch.png). A coordinate drag was attempted, but because Simulator pointer injection is not authoritative physical-touch input and did not produce a reliable logged placement delta, this remains PARTIAL rather than a passed drag/resize gate. The rebuilt deterministic layout smoke still reaches the original engine and records `editorPresented=true` in [`editor-persistence-system.log`](editor-persistence-system.log).

The Unreal menu split follow-up makes the distinction visible and semantic: the touch rail now exposes `MENU`, which sends Escape through the original SDL keyboard path, while the fixed three-dot host button remains the settings/import/diagnostics surface. A clean one-simulator `-UT99TouchSmokeTest -UT99MenuSmokeTest` run reached the original engine, installed the BC1 fallback, opened the original Unreal menu, and recorded `UT99TouchBridge action=pause pressed=true/false` in [`unreal-menu-split-system.log`](unreal-menu-split-system.log). [`unreal-menu-split-pass.png`](unreal-menu-split-pass.png) is the raw framebuffer evidence showing the original Unreal menu and both touch/menu surfaces. Physical hardware input remains unproven.

The network refresh used one clean simulator and the known unmodified endpoint `217.154.81.36:7777`. The engine received a compressed `DM-Liandri` map (`DLMGR ... COMPRESSION=1`), completed the server welcome, loaded the map, and possessed the player; the live frame is [`network-refresh-pass.png`](network-refresh-pass.png), with engine output in [`network-refresh-engine.stdout`](network-refresh-engine.stdout) and [`network-refresh-engine.log`](network-refresh-engine.log). This confirms current compressed-map interoperability with the BC1 fallback, but the exact historical `DM-Unreality][` package was not offered by the endpoint and remains an open targeted case.

The explicit `unreal://ut99.weba.ru:7777/DM-Unreality][` request was then repeated in a clean simulator run. The request path reached the endpoint and the native engine rendered the server's original MapVote/Welcome UI over a live scene with the GoldenPad rail, but the server rotated to `DM-Aim`; no `DM-Unreality][` package was delivered. See [`dm-unreality-request-pass.png`](dm-unreality-request-pass.png), [`dm-unreality-request-engine.stdout`](dm-unreality-request-engine.stdout), and [`dm-unreality-request-engine.log`](dm-unreality-request-engine.log). This remains a server-content availability limitation, not a renderer pass for that historical map.
